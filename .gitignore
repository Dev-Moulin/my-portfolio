node_modules
dist
build
.DS_Store
*.local
.env
.env.*
!.env.example
_backup

# Windows/WSL metadata files
*Zone.Identifier

# Graphify — sortie du knowledge graph (local, non versionné)
graphify-out/

# Télémétrie perf dev (jsonl locaux, cf. apps/web/perfPlugin.ts)
apps/web/perf-logs/

# Large GLB models — ignored EXCEPT the active production one (committed so GitHub Pages
# can serve it; the CI build needs it, no external hosting available)
apps/web/public/models/Spaceship_V5.*.glb
apps/web/public/models/Spaceship_V6*.glb
apps/web/public/models/Spaceship_New*.glb
!apps/web/public/models/Spaceship_NewV3.0_DracoKTX2.glb
apps/web/public/models/Collision_*.glb
apps/web/public/models/Sentinel_TRAIN.glb

# Documentation & config Claude Code (local, hors repo)
Claude/
.claude/
